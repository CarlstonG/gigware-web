import externalReview from "./external-review"
import onboarding from "./onboarding";
import provider from "./provider";
import settings from "./settings";

export default [].concat(
  externalReview,
  onboarding,
  provider,
  settings
)
